version: '3.8'

services:
  # ML Model API Service
  ml-api:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: ml-model-api
    ports:
      - "8000:8000"
    environment:
      - API_HOST=0.0.0.0
      - API_PORT=8000
      - DEBUG=false
    volumes:
      - ../models:/app/models:ro
      - ../logs:/app/logs
    restart: unless-stopped
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:8000/health" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - ml-network

  # Prometheus Monitoring
  prometheus:
    image: prom/prometheus:latest
    container_name: ml-prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus_data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.console.libraries=/etc/prometheus/console_libraries'
      - '--web.console.templates=/etc/prometheus/consoles'
      - '--web.enable-lifecycle'
    restart: unless-stopped
    networks:
      - ml-network

  # Grafana Dashboard
  grafana:
    image: grafana/grafana:latest
    container_name: ml-grafana
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin123
      - GF_USERS_ALLOW_SIGN_UP=false
    volumes:
      - grafana_data:/var/lib/grafana
      - ./grafana/provisioning:/etc/grafana/provisioning:ro
      - ./grafana/dashboards:/var/lib/grafana/dashboards:ro
    restart: unless-stopped
    networks:
      - ml-network
    depends_on:
      - prometheus

  # Redis for Caching (Optional)
  redis:
    image: redis:7-alpine
    container_name: ml-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    restart: unless-stopped
    networks:
      - ml-network
    command: redis-server --appendonly yes

  # Nginx Load Balancer (for scaling)
  nginx:
    image: nginx:alpine
    container_name: ml-nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    restart: unless-stopped
    networks:
      - ml-network
    depends_on:
      - ml-api

  # Autonomous Drift Monitoring Service
  # Runs continuously, checking for drift and triggering retraining
  # Lifecycle: Starts with docker-compose up, stops with docker-compose down
  drift-monitor:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: ml-drift-monitor
    command: python src/monitoring_service.py
    environment:
      - ENABLE_AUTOMATION=true # Set to false to disable
      - DRIFT_CHECK_INTERVAL=3600 # Check every hour (3600 seconds)
      - DRIFT_THRESHOLD=0.2 # Drift threshold for retraining
      - REFERENCE_DATA_PATH=data/dataset.csv
    volumes:
      - ../models:/app/models # Read/write access to models
      - ../data:/app/data:ro # Read-only access to data
      - ../logs:/app/logs # Write access to logs
    restart: unless-stopped
    networks:
      - ml-network
    depends_on:
      - ml-api
      - prometheus

volumes:
  prometheus_data:
  grafana_data:
  redis_data:


networks:
  ml-network:
    driver: bridge
